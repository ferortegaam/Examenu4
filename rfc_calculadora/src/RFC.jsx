import React from "react";

export const RFC = ({ rfc }) => {
  return (
    <div className="mt-4">
      <h4 className="text-center">RFC:</h4>
      <p className="text-center">{rfc}</p>
    </div>
  );
};
